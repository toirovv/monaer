import React, { useEffect } from 'react'

function Profile() {
  useEffect(() => {
    // Simple instant scroll
    window.scrollTo(0, 0);
  }, []);

  return (
    <div>
      
    </div>
  )
}

export default Profile
